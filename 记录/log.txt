2017-8-2-17:46 今天才检查到建边的时候，分隔作者明明是‘；’，居然用了split（‘，’）
不，是我自己搞错了。。确实作者代码是用‘，’分隔的。。

2017-8-4-17:20 果然格式不一致害死人。。。AAAI的数据写的时候用“；”分隔了作者代码，导致谁都没找到。。真坑。

还有合并这些csv的时候，因为文件目录不一样，明明os.listdir找到了文件，open的时候还得把前面的一串路径都加上而不是只用一个open（file）就行。得写open（‘一串路径’+file）。用if os.path.isfile()检查就更坑了，连错都不报一个，就是不执行。把这句注释了才发现是找不到要打开的file了。文件路径不一样千万小心啊。